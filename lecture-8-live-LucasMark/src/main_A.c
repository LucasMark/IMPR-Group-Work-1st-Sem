#include <stdio.h>
#include "planets.h"

int main(void) {
    int fprintf("planets.txt")

    // Lav, i main, et antal funktionskald til circumference med forskellige planeter som aktuelt parameter. Udskriv de returnerede værdier.
    printf("Hvilken omkreds vil du have? \n");
    //scanf( "%d", omkreds);
    circ(Venus);
    return 0;
}
